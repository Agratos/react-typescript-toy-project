const div = {
    vhCenter: `
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    `
};
const fontSize = {};
const fontFamily = {};

const theme = {
    div,
    fontSize,
    fontFamily,
};

export default theme;
